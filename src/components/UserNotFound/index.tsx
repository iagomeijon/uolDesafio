import styles from './styles.module.scss';

const UserCard = () => {
  return (
    <div className={styles.container}>
      <p className={styles.container__text}>Usuário Não encontrado :( </p>
    </div>
  );
};

export default UserCard;
